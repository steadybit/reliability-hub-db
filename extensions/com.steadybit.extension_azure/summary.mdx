# Introduction

The Azure (cloud computing platform run by Microsoft) extension bundles attack, target discovery for the Azure product pallet. At the moment you can use the Azure extension to change the state of virtual machines.

The Azure extension is essentially an adapter for the Azure APIs. It currently only supports a small set of Azure APIs. If you need additional capabilities, please consider contributing these directly via GitHub.
