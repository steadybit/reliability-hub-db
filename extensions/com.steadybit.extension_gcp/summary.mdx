# Introduction

The GCP (Google Cloud Platform) extension bundles attack, target discovery for the GCP product pallet.
At the moment you can use the GCP extension to change the state of virtual machines itself and of managed Kubernetes cluster.

The GCP extension is essentially an adapter for the GCP APIs. It currently only supports a small set of GCP APIs. If you need additional capabilities, please consider contributing these directly via GitHub.
