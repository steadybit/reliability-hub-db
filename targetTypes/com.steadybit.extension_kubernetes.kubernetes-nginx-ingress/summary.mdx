The Kubernetes NGINX Ingress target type allows you to discover and interact with NGINX Ingress in your Kubernetes cluster. This target type is useful for testing ingress configurations and network routing scenarios.

## Key Features

- Discover NGINX Ingress in your cluster
- Test ingress routing and load balancing
- Validate ingress configurations under various conditions
- Simulate network failures affecting ingress traffic