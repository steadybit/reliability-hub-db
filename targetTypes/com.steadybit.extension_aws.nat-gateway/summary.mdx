# Introduction

This extension discovers AWS NAT Gateways.

Once discovered, attributes such as the NAT Gateway id, ARN, VPC id, subnet id, connectivity type (public / private), state, and availability mode (multi-AZ / single-AZ) can be used for targeting.
