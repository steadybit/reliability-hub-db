# Introduction

This extension discovers Azure Event Grid topics (custom, system, and partner-kind).

Discovered attributes include the topic kind, input schema, public-network-access mode, whether local authentication is disabled, provisioning state, and the publicly reachable endpoint.
