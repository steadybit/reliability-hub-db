# Azure Cloud Discovery

With the Azure Cloud Discovery, you can easily discover cloud resources without having to install agents on every machine.

## Azure Scale Set Instance Discovery
The extension discovers all your Azure scale set instances / virtual machines and create a scale set instance target for each one of them.