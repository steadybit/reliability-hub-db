# Cloud Run Service Discovery

The extension discovers Cloud Run services across accessible projects and locations. Each service is exposed as a target with attributes for its location, ingress mode, latest ready revision, traffic split, and image reference.
