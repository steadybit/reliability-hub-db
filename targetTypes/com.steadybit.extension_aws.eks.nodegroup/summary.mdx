# Introduction

This extension discovers AWS EKS managed node groups. Once discovered, attributes such as the node group ARN, parent cluster, AMI type, capacity type (ON_DEMAND / SPOT), instance types, scaling configuration (min/max/desired), and the underlying Auto Scaling group(s) can be used for targeting.
