# Introduction

This extension discovers Azure Kubernetes Service (AKS) managed clusters.

Once discovered, cluster-wide attributes such as Kubernetes version, power state, private-cluster mode, authorized IP ranges, network plugin / policy, SKU tier, Azure RBAC + AAD configuration, and the managed node resource group can be used for targeting.
