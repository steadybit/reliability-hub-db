# Introduction

This extension discovers Event Grid event subscriptions and tags each with its parent topic.

Discovered attributes include the destination endpoint type (Webhook, Service Bus, Storage Queue, ...), delivery schema, retry policy (max delivery attempts, event TTL), and whether a dead-letter destination is configured.
