# Introduction

This extension discovers Azure Storage Queues, scoped to their parent storage account.

Discovered attributes include the storage account name, kind, SKU and tier, public-network-access mode, blob-public-access and shared-key access settings, minimum TLS version, replication settings, and the queue service endpoint.
