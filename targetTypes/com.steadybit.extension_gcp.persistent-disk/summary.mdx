# GCP Persistent Disk Discovery

The extension discovers zonal and regional Persistent Disks across accessible projects. Each disk is exposed as a target with attributes for its type (pd-standard, pd-balanced, pd-ssd, pd-extreme, hyperdisk variants), size, source image, encryption mode, and the VM instance(s) it is attached to.
