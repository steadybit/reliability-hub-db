# Introduction

This extension discovers AWS EventBridge rules across all event buses (default and custom).

Once discovered, attributes such as the rule ARN, event bus name, schedule expression, event pattern, state (ENABLED / DISABLED), and the list of target ids can be used for targeting.
