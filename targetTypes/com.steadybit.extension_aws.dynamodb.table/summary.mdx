# Introduction

This extension discovers AWS DynamoDB tables.

Once discovered, attributes such as the table name, ARN, billing mode (PROVISIONED / PAY_PER_REQUEST), table class, continuous backups status, TTL status, and the names of attached global secondary indexes can be used for targeting.
