# Introduction

To facilitate actions against RDS clusters, this extension discovers AWS RDS cluster. Once discovered, properties such as the cluster ID, its engine, the ARN and others can be used for targeting.
