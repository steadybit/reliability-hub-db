# Introduction

This extension discovers AWS EBS volumes.

Once discovered, attributes such as the volume id, ARN, volume type (gp3, io2, st1, ...), size, state, encryption status, KMS key id, and the EC2 instance the volume is attached to can be used for targeting.
