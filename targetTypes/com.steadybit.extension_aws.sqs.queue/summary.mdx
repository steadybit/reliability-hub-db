# Introduction

This extension discovers AWS SQS queues (standard and FIFO).

Once discovered, attributes such as the queue name, ARN, URL, queue type, server-side encryption mode (SQS-managed / KMS), and current visibility timeout can be used for targeting.
