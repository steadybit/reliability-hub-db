# Introduction

This extension discovers Azure Cosmos DB accounts.

Discovered attributes include the API kind (SQL, Mongo, Cassandra, Gremlin, Table), consistency level, write-region configuration (multi-region writes, automatic failover), the list of geo-replicated locations and their failover priority, backup policy, and the public-network-access mode.
