# Introduction

This extension discovers Azure Load Balancer instances (Standard and Basic SKU).

Discovered attributes include the SKU name and tier, whether the load balancer has a public frontend, frontend availability zones, and the number of backend pools, load-balancing rules, inbound NAT rules, probes, and outbound rules.
