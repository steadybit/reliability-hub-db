# Introduction

This extension discovers AKS managed node pools and tags each with its parent cluster.

Discovered attributes include the node pool mode (System / User), VM size, OS type and SKU, scale-set priority and eviction policy, autoscaling bounds, current count, and the pool's provisioning + power state.
