# Pub/Sub Subscription Discovery

The extension discovers Pub/Sub subscriptions across accessible projects. Each subscription is exposed as a target with attributes for its parent topic, delivery type (pull, push, BigQuery, Cloud Storage), ack deadline, message retention duration, dead-letter policy, and filter expression.
