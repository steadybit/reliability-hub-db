# Discovery of Kubernetes HAProxy Ingress

Kubernetes HAProxy-based ingress-controllers are discovered as a target for HAProxy-based ingresses attacks like `Block Traffic` or `Delay Traffic` on a specific HTTP path / HTTP Method / HTTP Headers in the ingress controller.