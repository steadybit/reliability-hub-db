# Cloud Spanner Instance Discovery

The extension discovers Cloud Spanner instances across accessible projects. Each instance is exposed as a target with attributes for its instance config (regional / multi-regional), edition, processing units or node count, autoscaling configuration, and state.
