# Introduction

This extension discovers AWS Elastic Kubernetes Service (EKS) clusters. Once discovered, cluster-wide attributes such as the cluster ARN, Kubernetes version, status, endpoint reachability (public/private), authentication mode, IAM/OIDC issuer, and VPC configuration can be used for targeting.
