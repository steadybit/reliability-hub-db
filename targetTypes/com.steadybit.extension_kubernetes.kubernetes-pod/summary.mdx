# Introduction

Kubernetes pods are discovered as a target for pod-based kubernetes attacks like `Delete Pod`. Kubernetes pod targets includes attributes describing their containers and other usefull attributes like the host where the pod is scheduled on.