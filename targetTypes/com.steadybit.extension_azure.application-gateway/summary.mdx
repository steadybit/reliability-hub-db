# Introduction

This extension discovers Azure Application Gateway instances.

Discovered attributes include the SKU name and tier, fixed and autoscale capacity bounds, operational and provisioning state, HTTP/2 enablement, WAF configuration, frontend port and listener count, and backend pool count.
