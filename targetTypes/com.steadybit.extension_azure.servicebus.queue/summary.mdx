# Introduction

This extension discovers queues within each Service Bus namespace and tags each queue with its parent namespace.

Discovered attributes include the queue status (Active / Disabled / SendDisabled / ReceiveDisabled), max delivery count, dead-letter-on-expiration setting, duplicate-detection requirement, session enablement, partitioning, and lock / message TTL durations.
