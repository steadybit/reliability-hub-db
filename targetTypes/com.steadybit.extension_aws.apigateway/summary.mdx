# Introduction

This extension discovers AWS API Gateways for both REST APIs (v1) and HTTP APIs (v2). Each stage of an API is exposed as one target, so per-environment throttle and logging settings are addressable individually. Attributes such as the API id, API name, protocol type, stage name, deployment id, throttle limits, and access-log configuration can be used for targeting.
