# Introduction

This extension discovers AWS Lambda functions and registers them as targets. It collects metadata, such as function name, Amazon Resource Name (ARN), runtime and other attributes — and exposes these properties for targeting and action execution.
