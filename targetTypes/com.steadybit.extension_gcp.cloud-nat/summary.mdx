# GCP Cloud NAT Discovery

The extension discovers Cloud NAT gateways attached to Cloud Routers across the projects and regions the extension has access to. Each discovered NAT is exposed as a target with attributes for its router, region, source subnet-range mode, attached subnetworks, and NAT IP allocation strategy.
