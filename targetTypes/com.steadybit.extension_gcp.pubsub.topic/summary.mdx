# Pub/Sub Topic Discovery

The extension discovers Pub/Sub topics across accessible projects. Each topic is exposed as a target with attributes for its message retention duration, message-storage-policy allowed regions, KMS key name (if CMEK-encrypted), schema settings, and ingestion source.
