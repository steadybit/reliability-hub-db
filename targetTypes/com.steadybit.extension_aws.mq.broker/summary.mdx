# Introduction

This extension discovers AWS managed Amazon MQ brokers.

Once discovered, attributes such as the broker ARN, broker id, engine type (ActiveMQ / RabbitMQ), engine version, deployment mode (SINGLE_INSTANCE, ACTIVE_STANDBY_MULTI_AZ, CLUSTER_MULTI_AZ), broker state, and host instance type can be used for targeting.
