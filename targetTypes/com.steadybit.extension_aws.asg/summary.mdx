# Introduction

This extension discovers AWS EC2 Auto Scaling Groups (ASGs). Once discovered, attributes such as the ASG name, ARN, availability zones, desired/min/max capacity, launch template, load balancer target groups, and currently suspended processes can be used for targeting.
